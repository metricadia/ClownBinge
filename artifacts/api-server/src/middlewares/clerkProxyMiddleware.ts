// Clerk removed — self-hosted JWT auth in use.
// This stub is retained to prevent import errors from any cached references.
export const CLERK_PROXY_PATH = "/api/__clerk";
export const clerkProxyMiddleware = () => (_req: any, _res: any, next: () => void) => next();
