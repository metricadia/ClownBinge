/**
 * Admin Kemet8 auth bridge.
 * The primary admin auth flow is OTP-based (admin-otp.ts).
 * This file is kept for any future non-Clerk admin utilities.
 */
import { Router } from "express";

const router = Router();

export default router;
